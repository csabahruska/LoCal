date: 2026-01-21

PROBLEM: end witness availability is not tracked by the type system, neither the end-witness requirement property
DONE: extend the type system to track these too

TODO:
  frontend:
    - linear region inference for lets
    - think through hole fixing machinery ; and hole introduction and region refreshing during hole filling
  backend:
    - handle fun arg end witness passing
    - design where to handle GenEW a) backend b) hical to local conversion ; DONE a)

  - try out bitpacking??
    PROBLEMS:
      - we would need alignment control
        IDEA:
          + aligned region buffer start
          + field cursor would automatically adjusted according to the alignment constraint (this is runtime)
          Q: can we do this statically? how?

TODO:
  - CANCEL: port examples to CPS
  - CANCEL: add linear arrows
  done - port everything back to direct value based IR, it is ok because (CPS style) MkStaticEW solves the end-witness effect problem
  wip - pass/return fun arg end-witnesses

  done - fix pointer deref types in the model ; deref location starts a new region
    + check what is the deref type in gibbon
  done - support multi parameter functions in the model ; but only when it is easy
  - check unexecuted alloc actions at the end of codegen or functions
  - all cursor gen/get functions must be cps style which can postpone actions
    this will implicitly implement the new backend design where the cursors are generated in the location order
  - bring back cps style, or check that a location is generated for write only once
    Q: if we would keep the direct style then would the cursor generation work in direct mode?
    A: i think so
    IDEA: write is direct style with direct codegen, reads could be suspended
  - handle funapp multi end-witness returning

what works:
  - location calculus EDSL model where the location are inferred by the idris type elaborator
  - sharing and indirection support
  - c backend
  - ADTs are modeled as Sum Of Products (SOP)
  - recursive functions
  - recursive data types

limitations:
  - byte sized tags ; either should have one bit sized tag
  - only I64 primitive type
  - hical to local conversion is very basic and generates traversals and non optimal region allocation

FUTURE WORK:
  - SKIP: refactor to CPS style EDSL to prevent codegen duplications
  done - end-witness tracking in the type system ; functions return end-witnesses of the traversed input values
      ideas: a) use linear variables b) index Exp with traverse effect environment
  done - multi parameter functions
  - index Loc with Ty for more safety

  - sub byte addressing ; this is equal to bit packed data
  - pack multiple tags into a byte or machine word
  - more primitive types and primops, e.g. arbitrary sized words, byte-buffer
  - add write-barrier primitive that could give access to already written data for reading
    like a written sub-region in a parent region which is partially complete
